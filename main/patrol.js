"use strict";
const pogobuf = require("pogobuf");

class Patrol {
	constructor(account) {
		this.account = account;
	}

	setPoints(points) {
		this.points = points;
	}

	start() {
		
	}
}

module.exports = Patrol;